{
  "name": "@romanczuk/blog-mcp-server",
  "version": "0.1.0",
  "description": "Local MCP server that lets agents draft blog posts to Sanity. Draft-only — never publishes directly.",
  "private": true,
  "type": "module",
  "bin": {
    "blog-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@sanity/client": "^7.16.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.0",
    "typescript": "^5"
  }
}
